THE start.bat can start the program automatically without manually using terminal it will also hide the terminal.
the start_debug.bat does the same thing but without hiding the terminal

the build.bat will use Installer to compile it all into a clean exe file